# 运行指南 — AI Agent 财务报销审核系统

## 1. 环境要求

- **操作系统**：Windows 10+ / macOS 10.15+ / Ubuntu 18.04+
- **Python**：3.10 或更高版本
- **Node.js**：18.0 或更高版本
- **内存**：至少 8GB RAM
- **存储**：至少 10GB 可用空间

> 注：开发环境使用 SQLite 数据库，无需额外安装数据库服务。

## 2. 项目目录

```
D:\python\agent项目\expense-audit-system\
├── backend\          # 后端 FastAPI 服务
├── frontend\         # 前端 Vue3 应用
├── README.md         # 项目说明
└── 运行指南.md        # 本文件
```

## 3. 后端部署

### 3.1 进入后端目录

```bash
cd D:\python\agent项目\expense-audit-system\backend
```

### 3.2 创建 Python 虚拟环境

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/macOS
python3 -m venv venv
source venv/bin/activate
```

### 3.3 安装依赖

```bash
pip install -r requirements.txt
```

### 3.4 配置环境变量

项目已包含 `.env` 配置文件，包含以下关键配置：

| 配置项 | 说明 |
|--------|------|
| `DEEPSEEK_API_KEY` | DeepSeek API 密钥 |
| `DEEPSEEK_API_BASE` | DeepSeek API 地址 |
| `MODEL_NAME` | LLM 模型名称 (deepseek-v4-flash) |
| `DASHSCOPE_API_KEY` | 千问 DashScope API 密钥 |
| `DASHSCOPE_API_BASE` | 千问 API 地址 |
| `EMBEDDING_MODEL` | 向量模型 (text-embedding-v1) |
| `DATABASE_URL` | 数据库连接 (默认 SQLite) |
| `JWT_SECRET_KEY` | JWT 签名密钥 |
| `SECRET_KEY` | 应用密钥 |

如需修改配置，编辑 `backend/.env` 文件。

### 3.5 启动后端服务

```bash
# 开发模式（热重载）
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 生产模式
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### 3.6 验证后端

- **健康检查**：http://localhost:8000/health
- **API 文档**：http://localhost:8000/api/docs
- **根路径**：http://localhost:8000/

## 4. 前端部署

### 4.1 进入前端目录

```bash
cd D:\python\agent项目\expense-audit-system\frontend
```

### 4.2 安装依赖

```bash
npm install
```

### 4.3 启动开发服务器

```bash
npm run dev
```

前端默认运行在 http://localhost:5173

### 4.4 生产构建（可选）

```bash
npm run build
npm run preview
```

## 5. 访问地址

| 服务 | 地址 |
|------|------|
| 前端应用 | http://localhost:5173 |
| API 文档 (Swagger) | http://localhost:8000/api/docs |
| API 文档 (ReDoc) | http://localhost:8000/api/redoc |
| 健康检查 | http://localhost:8000/health |

## 6. 默认账号

| 角色 | 用户名 | 密码 | 权限 |
|------|--------|------|------|
| 管理员 | admin | admin123 | 全部权限 |
| 财务人员 | finance01 | finance123 | 审批、查看全部、导出 |
| 财务人员 | finance02 | finance123 | 审批、查看全部、导出 |
| 部门经理 | manager01 | manager123 | 审批、查看部门 |
| 部门经理 | manager02 | manager123 | 审批、查看部门 |
| 普通员工 | employee01 | employee123 | 提交、查看自己的报销 |
| 普通员工 | employee02 | employee123 | 提交、查看自己的报销 |
| 普通员工 | employee03 | employee123 | 提交、查看自己的报销 |

## 7. API 接口概览

### 认证接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/auth/login | 用户登录 |
| POST | /api/v1/auth/register | 用户注册 |
| GET | /api/v1/auth/me | 获取当前用户信息 |
| POST | /api/v1/auth/logout | 退出登录 |

### 报销接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/expenses | 获取报销列表 |
| POST | /api/v1/expenses | 创建报销单 |
| GET | /api/v1/expenses/{id} | 获取报销详情 |
| PUT | /api/v1/expenses/{id} | 更新报销单 |
| DELETE | /api/v1/expenses/{id} | 删除报销单 |
| POST | /api/v1/expenses/{id}/submit | 提交报销单 |
| POST | /api/v1/expenses/{id}/withdraw | 撤回报销单 |
| POST | /api/v1/expenses/{id}/ai-review | AI 审核报销 |
| POST | /api/v1/expenses/upload-invoice | 上传发票 |
| GET | /api/v1/expenses/statistics | 报销统计 |

### 审批接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/approvals | 获取审批列表 |
| POST | /api/v1/approvals/{id}/approve | 批准报销 |
| POST | /api/v1/approvals/{id}/reject | 拒绝报销 |
| GET | /api/v1/approvals/{id}/history | 审批历史 |

### Agent 接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/agent/execute | 执行 AI Agent 审核 |

### 报表接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/reports/summary | 汇总统计 |
| GET | /api/v1/reports/by-type | 按类型统计 |
| GET | /api/v1/reports/by-department | 按部门统计 |
| GET | /api/v1/reports/trend | 趋势分析 |

## 8. 模型配置说明

本系统使用以下 AI 模型：

### LLM 大语言模型
- **模型**：DeepSeek V4 Flash (`deepseek-v4-flash`)
- **API 地址**：https://api.deepseek.com/v1
- **用途**：智能审核决策、文档解析、风险分析、文本生成

### Embedding 向量模型
- **模型**：千问 text-embedding-v1
- **API 地址**：https://llm-pszva1pkca2z60ha.cn-beijing.maas.aliyuncs.com/api/v1
- **用途**：文本向量化、RAG 知识检索

## 9. 常见问题

### Q: 后端启动报错 "ModuleNotFoundError"
确保已激活虚拟环境并安装了所有依赖：
```bash
venv\Scripts\activate
pip install -r requirements.txt
```

### Q: 前端无法连接后端 API
检查：
1. 后端是否在 8000 端口运行
2. 前端 `.env.example` 中 API 地址是否正确
3. 浏览器控制台是否有 CORS 错误

### Q: SQLite 数据库在哪里？
数据库文件位于 `backend/data/expense_audit.db`，首次启动会自动创建。

### Q: 如何重置数据库？
删除 `backend/data/expense_audit.db` 文件后重启后端即可重新初始化。

### Q: 向量数据库存储位置？
ChromaDB 数据存储在 `backend/data/chroma/` 目录下。

## 10. 技术架构

```
用户浏览器 (Vue3 + Element Plus)
        |
    HTTP/REST API
        |
   FastAPI 后端服务
        |
   ┌────┼────┬────┬────┐
   │    │    │    │    │
 认证  报销  审批  Agent 报表
   │    │    │    │    │
   └────┴────┴────┴────┘
        |
   ┌────┼────────┐
   │    │         │
SQLite  ChromaDB  LLM/Embedding
(业务数据) (向量)   (AI服务)
```

### Agent 工作流 (LangGraph)

```
文档解析 → 规则校验 → 风险评估 → 知识检索 → 智能决策
(Document) (Rule)   (Risk)    (RAG)     (Decision)
```

## 11. 项目特色

1. **DeepSeek V4 Flash 驱动**：使用最新的 DeepSeek 大模型进行智能审核
2. **千问向量嵌入**：高性能文本向量化，支持精准知识检索
3. **多 Agent 协作**：5 个专业 Agent 通过 LangGraph 协同工作
4. **RAG 增强**：结合企业知识库提升审核准确性
5. **现代化 UI**：Vue3 + Element Plus 响应式界面
6. **一键启动**：SQLite 零配置，开箱即用
